zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: gateway
    api-b:
      path: /api-b/**
      serviceId: test
# swagger配置 
api-params:
    services：
      - name: 测试服务
        location: /test/v2/api-docs
   
